#- Kernel
watching files - maybe just a better way to implement polling (see select(2) and poll(2))
better error handling (so the kernel doesn't crash)
setuid
users & groups (perm fs)
validation of parameters to syscalls
oc signal fs
make type-mux-demux-fs apis and streams not store their data based on their path

#- Userspace
devbus
shell
